[pytest]
testpaths = tests
asyncio_mode = auto
log_cli = false
log_level = WARNING
addopts =
    -v
    --tb=short
    --strict-markers
    -p no:warnings
markers =
    slow: marks tests as slow (deselect with -m 'not slow')
    integration: marks tests requiring live API keys
